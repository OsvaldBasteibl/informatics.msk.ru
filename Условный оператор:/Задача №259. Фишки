//В каждую крайнюю клетку квадратной доски поставили по фишке. Могло
//ли оказаться, что выставлено ровно k фишек? (Например, если доска
//2×2, то выставлено 4 фишки, а если 6×6 - то 20).
//Входные данные
//Вводится одно натуральное число k, не превосходящее 30000
//Выходные данные
//Программа должна вывести слово YES, если существует такой размер
//доски, на который будет выставлено ровно (не больше, и не меньше) k
//фишек, в противном случае - вывести слово NO.
//Решение
import java.util.Scanner;
public class Chips {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int k;
        k = input.nextInt();
        if(k == 1 || k % 4 == 0){
            System.out.println("YES");
        }else {
            System.out.println("NO");
        }
    }
}
